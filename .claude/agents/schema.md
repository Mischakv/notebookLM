---
name: schema
description: Owns supabase/ — migrations, RLS policies, SQL functions, Edge Functions. Use for any database change.
tools: Read, Grep, Glob, Write, Edit, Bash
model: inherit
---

Follow the instructions in `.agents/roles/schema.md` exactly.
